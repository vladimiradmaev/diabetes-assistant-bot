# ДиаАИ (DiAI) - Умный помощник для диабетиков

Telegram-бот для анализа еды и расчета инсулина для диабетиков.

> ❤️ **Создан диабетиком для диабетиков** с пониманием реальных потребностей в управлении заболеванием.
> 
> ⚠️ **BETA версия** - бот находится в стадии активной разработки и тестирования. Все рекомендации носят справочный характер. Всегда консультируйтесь с врачом!

## Возможности

- 📸 Анализ фотографий еды с помощью Gemini AI
- 🍎 Определение продуктов и расчет углеводов
- 💉 Автоматический расчет дозы инсулина
- ⚖️ Оценка веса порций
- ⏰ Настройка инсулиновых коэффициентов по времени

## Быстрый запуск

1. Скопируйте `.env.example` в `.env` и заполните переменные:
```bash
cp .env.example .env
```

2. Запустите приложение:
```bash
./run.sh
```

Это автоматически:
- Соберет Docker образы
- Запустит PostgreSQL и приложение
- Выполнит миграции базы данных
- Покажет логи

## Переменные окружения

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# AI Services
GEMINI_API_KEY=your_gemini_api_key

# Database
DB_HOST=postgres
DB_PORT=5432
DB_USER=diabetes_user
DB_PASSWORD=diabetes_password
DB_NAME=diabetes_db
```

## Команды

```bash
make run      # Запустить приложение
make dev      # Запустить в режиме разработки
make logs     # Показать логи
make stop     # Остановить приложение
make clean    # Очистить все данные
```

## Использование

1. Отправьте боту фото еды
2. При необходимости укажите вес порции
3. Получите анализ с расчетом углеводов и инсулина
4. Настройте инсулиновые коэффициенты через команды бота

## Архитектура

- **Go 1.22+** с новым ServeMux
- **PostgreSQL** для хранения данных
- **Gemini AI** для анализа изображений
- **Docker** для контейнеризации
- **Telegram Bot API** для интерфейса

## Основные функции

### 🆕 Последние обновления
- **Улучшенный AI**: Переход на Gemini 2.0 Flash для более точного анализа
- **Защита от ошибок**: Автоматическое распознавание неподходящих изображений
- **Стабильность**: Улучшена обработка ошибок

### 🍽️ Анализ еды
- Отправьте фото блюда для анализа
- Бот определит состав блюда, углеводы и ХЕ
- Можно указать вес блюда в подписи к фото
- Если вес не указан, бот попробует оценить его автоматически
- Показывает уверенность в определении блюда
- Рассчитывает рекомендуемую дозу инсулина на основе ваших коэффициентов
- Защита от неподходящих изображений - бот распознает, если на фото нет еды
- Использует Gemini 2.0 Flash для более точного и быстрого анализа

### ⚙️ Настройки
#### Коэффициент инсулина на ХЕ
- Настройка коэффициентов инсулина для разных периодов времени
- Поддержка нескольких временных периодов в течение суток
- Визуализация покрытия 24-часового периода
- Возможность добавления, редактирования и удаления коэффициентов
- Автоматическое объединение периодов при редактировании

## Как использовать

### Анализ еды
1. Нажмите кнопку "🍽️ Анализ еды" в главном меню
2. Отправьте фото блюда
3. (Опционально) Укажите вес в граммах в подписи к фото
4. Получите анализ с информацией:
   - Углеводы (г)
   - Хлебные единицы (ХЕ)
   - Рекомендуемая доза инсулина
   - Вес блюда
   - Уверенность в определении
   - Подробное описание анализа

### Настройка коэффициентов инсулина
1. Нажмите "⚙️ Настройки" в главном меню
2. Выберите "📊 Коэф. на ХЕ"
3. Нажмите "➕ Добавить" для создания нового периода
4. Введите период времени в формате ЧЧ:ММ-ЧЧ:ММ (например, 08:00-12:00)
5. Введите коэффициент (количество единиц инсулина на 1 ХЕ)
6. Повторите для других периодов времени

## Технические детали

### Используемые технологии
- Go
- Telegram Bot API
- Google Gemini 2.0 Flash / OpenAI для анализа изображений
- PostgreSQL для хранения данных

### Структура проекта
```
.
├── cmd/
│   └── bot/                 # Точка входа приложения
├── internal/
│   ├── bot/                 # Логика Telegram бота
│   ├── database/           # Работа с базой данных
│   └── services/           # Бизнес-логика
│       ├── food_analysis/  # Анализ еды
│       ├── insulin/        # Управление инсулином
│       └── user/           # Управление пользователями
├── configs/                # Конфигурационные файлы
└── README.md
```

## Быстрый запуск

### 🚀 Одна команда для запуска всего

```bash
# Клонируйте репозиторий
git clone https://github.com/vladimiradmaev/diabetes-helper.git
cd diabetes-helper

# Создайте файл .env с вашими токенами
cp .env.example .env
# Отредактируйте .env файл

# Запустите всё одной командой
./run.sh
```

### 📋 Альтернативные способы запуска

#### Через Makefile (рекомендуется для разработки)
```bash
# Показать все доступные команды
make help

# Запустить приложение с базой данных
make run

# Запустить в режиме разработки (с выводом логов)
make dev

# Остановить приложение
make stop

# Показать логи
make logs

# Локальная разработка (только БД в Docker)
make local-run
```

#### Через Docker Compose
```bash
# Запуск в фоне
docker-compose up -d

# Запуск с выводом логов
docker-compose up

# Остановка
docker-compose down
```

## Установка и настройка

### Требования
- Docker и Docker Compose
- Git

### Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Обязательные переменные
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
GEMINI_API_KEY=your_gemini_api_key

# Опциональные переменные базы данных (есть значения по умолчанию)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=diabetes_helper
```

### Получение токенов

1. **Telegram Bot Token:**
   - Напишите [@BotFather](https://t.me/botfather) в Telegram
   - Создайте нового бота командой `/newbot`
   - Скопируйте полученный токен

2. **Gemini API Key:**
   - Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
   - Создайте новый API ключ

## Ограничения

- Бот может анализировать только одно фото за раз
- Для анализа нескольких блюд нужно отправлять каждое фото отдельным сообщением
- Точность определения блюд зависит от качества фото и возможностей AI
- Рекомендации по инсулину являются приблизительными и требуют проверки

## История изменений

### v0.3.0 (Текущая версия)
- 🚀 **Обновление AI**: Переход на Gemini 2.0 Flash для более быстрого и точного анализа
- 🛡️ **Защита от ошибок**: Автоматическое распознавание неподходящих изображений (не еда)
- 🐛 **Исправления**: Устранена проблема с обработкой одиночных фотографий
- 📝 **Документация**: Обновлено описание проекта и функционала

### v0.2.0
- 💉 **Инсулиновые рекомендации**: Расчет рекомендуемых доз инсулина
- ⚙️ **Настройки коэффициентов**: Управление инсулиновыми коэффициентами по времени
- 🥖 **Хлебные единицы**: Автоматический расчет ХЕ (углеводы/12)


### v0.1.0
- 🍽️ **Анализ еды**: Базовый анализ фотографий еды с помощью AI
- 🔢 **Расчет углеводов**: Определение количества углеводов в блюдах
- 👤 **Управление пользователями**: Регистрация и хранение данных пользователей
- 🗄️ **База данных**: PostgreSQL для хранения данных

> 📋 **Полная история изменений** доступна в [GitHub Releases](https://github.com/vladimiradmaev/diabetes-helper/releases)

## Лицензия

MIT 

## 📋 Логирование

Бот включает комплексную систему логирования:

### Возможности логирования
- **Сохранение в файлы** в директории `logs/` с ежедневной ротацией (`diabetes-helper-YYYY-MM-DD.log`)
- **Вывод в консоль** с временными метками и уровнями логирования
- **Отслеживание всех операций** включая API вызовы, ошибки, действия пользователей
- **Разные уровни логирования**: INFO, ERROR, WARNING, DEBUG

### Уровни логирования

- **INFO**: Общий поток приложения (запуск бота, действия пользователей, успешные операции)
- **ERROR**: Ошибки, требующие внимания (сбои API, ошибки парсинга)  
- **WARNING**: Важные, но некритичные проблемы (превышение лимитов, переключения)
- **DEBUG**: Детальная информация для отладки (ответы API, внутреннее состояние)

### Расположение файлов логов

```
logs/
├── diabetes-helper-2024-01-15.log
├── diabetes-helper-2024-01-16.log
└── ...
```

### Просмотр логов

```bash
# Просмотр логов текущего дня
tail -f logs/diabetes-helper-$(date +%Y-%m-%d).log

# Просмотр всех ошибок
grep "\[ERROR\]" logs/*.log

# Просмотр активности конкретного пользователя
grep "user 12345" logs/*.log

# Поиск проблем с API
grep "429\|quota\|rate limit" logs/*.log
```

## 🚨 Устранение неполадок

### Ошибка 429 (превышение квоты API)

Если вы получаете ошибку `googleapi: Error 429: You exceeded your current quota`, это означает превышение лимитов бесплатного уровня Gemini API.

**Текущие лимиты Gemini 2.0 Flash (Free Tier):**
- 15 запросов в минуту
- 1,500 запросов в день
- 1,000,000 токенов в минуту

**Автоматические решения в боте:**
1. **Retry с экспоненциальной задержкой** - бот автоматически повторяет запросы с увеличивающейся задержкой
2. **Fallback на OpenAI** - при превышении лимитов Gemini бот автоматически переключается на OpenAI
3. **Информирование пользователя** - бот уведомляет о переключении между моделями

**Ручные решения:**
1. **Подождать** - лимиты сбрасываются каждую минуту/день
2. **Обновить до Paid Tier** - подключить биллинг в Google Cloud Console
3. **Использовать OpenAI** - временно переключиться на OpenAI API

**Мониторинг использования:**
- Проверяйте логи бота для отслеживания количества запросов
- Используйте Google Cloud Console для мониторинга квот 